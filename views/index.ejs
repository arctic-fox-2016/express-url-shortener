<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% if(type ==0){%>

    <!-- <% } %> -->

    <!-- <div>
    <a href='/urls'>Add Url</a>
    </div> -->
    Link you have accessed :
    <ul id="link-list">
    <% for (var i=0; i<list.length; i++) {%>
    <li><%= list[i].link %>  ||   count : <%=list[i].click_count%></li>
    <% } %>
  </ul>

  <h1>URL Shortener</h1>
  <form action ='/urls' method='POST'>
    <input type='text' name='link'/>
    <button type='submit'>Shorten url</button>
  </form>
  <ul>
  <% for (var i=0; i<list.length; i++) { %>
  <li> <%= list[i].link %> shortening to <a href="/<%= list[i].name %>">h8/<%=list[i].name%></a>
      <% } %>
  </li>

  </body>
</html>
