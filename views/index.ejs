<!DOCTYPE HTML>
<head>
</head>
<body>
  <p>Halo</p>
  <% if (word){
    for (var i in word){ %>
      <li><a href = "/red/<%=word[i].id%>" >andrewly<%=word[i].id %>|      click_count: <%=word[i].click_count%></a>
    <% }
  }
  %>
  <form action="/urls" method="POST">
    <input type = "text" name = "url">
    <input type = "submit">
  </form>
</body>
</html>
