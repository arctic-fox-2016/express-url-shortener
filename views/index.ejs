<!DOCTYPE HTML>
<head>
</head>
<body>
  <p>Halo</p>
  <% if (word){
    for (var i in word){ %>
      <li><a href = "/red/<%=word[i].id%>" >andrewly<%=word[i].id %>|</a>      click_count: <%=word[i].click_count%>
    <% }
  }
  %>
  <form action="/urls" method="POST">
    <input type = "text" name = "url">
    <input type = "submit">
  </form>
</body>
</html>
